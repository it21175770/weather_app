// lib/features/home/home_view_model.dart
// Optional wrapper around provider – can be extended later.
// For now this just exists to match MVVM naming.
class HomeViewModel {
  // You can put view-specific logic here if needed.
}
